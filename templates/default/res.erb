# Generated by Chef

resource <%= @resource %> {
  device    <%= @device %>;
  disk      <%= @disk %>;
  meta-disk internal;
  on <%= @local_host %> {
    address   <%= @local_ip %>:<%= @port %>;
  }
  on <%= @remote_host %> {
    address   <%= @remote_ip %>:<%= @port %>;
  }
}
